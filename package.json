{
  "name": "@cardboardrobots/merge",
  "version": "0.0.4",
  "description": "",
  "repository": {
    "url": "https://github.com/cardboardrobots/merge"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "test": "jest",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.2.0",
    "jest": "^29.2.1",
    "ts-jest": "^29.0.3",
    "typescript": "^4.8.4"
  },
  "jest": {
    "preset": "ts-jest",
    "testPathIgnorePatterns": [
      "<rootDir>/dist/"
    ]
  }
}
